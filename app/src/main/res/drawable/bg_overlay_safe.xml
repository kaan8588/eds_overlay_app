<?xml version="1.0" encoding="utf-8"?>
<!-- Overlay safe status card (opaque with defined border) -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/overlay_safe_bg" />
    <corners android:radius="@dimen/radius_surface" />
    <stroke
        android:width="1dp"
        android:color="@color/overlay_safe_stroke" />
</shape>
